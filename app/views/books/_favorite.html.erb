
  <% if book.favorited_by?(current_user) %>
            <p>
              <%= link_to book_favorite_path(book), method: :delete do %>
                <i class="fa-solid fa-heart like-btn" ><%= book.favorites.count %></i>
              <% end %>
            </p>
            <% else %>
            <p>
              <%= link_to book_favorite_path(book), method: :post do %>
                <i class="fa-solid fa-heart unlike-btn"></i><%= book.favorites.count %> 
              <% end %>
            </p>
          <% end %>
                    